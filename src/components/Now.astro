---
// DRAFT — same straiv sign-off gate as the hardware-integration case study;
// confirm the "3,400+ hotels" figure and phrasing are fine to publish before shipping.
---

<section id="now" class="now container">
  <h2>Now</h2>
  <p>
    I'm a frontend developer at <strong>straiv</strong>, a platform used by 3,400+ hotels to run
    guest-facing operations, including check-in kiosks, ID scanning, and keycard issuance.
  </p>
</section>

<style>
  .now {
    padding-block: var(--space-2xl);
    border-top: 1px solid var(--color-border);
  }

  h2 {
    font-size: 1.5rem;
    font-weight: 600;
    margin: 0 0 var(--space-md);
  }

  p {
    max-width: 40rem;
    color: var(--color-foreground);
  }
</style>
